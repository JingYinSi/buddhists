
describe('biz', () => {
	const ID_NOT_EXIST = '5ce79b99da3537277c3f3b66'
	let schema, testTarget, toCreate;
	let id, __v;

	beforeEach(function (done) {
		__v = 0
		return clearDB(done);
	})

	it('用例场景说明', () => {
		expect(1, 1)
	})
})